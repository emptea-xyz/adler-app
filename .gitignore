# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment / secrets
.env
.env.*
!.env.example

# Expo
.expo/
.expo-shared/

# Native build output (regenerated by `npx expo prebuild --clean`)
ios/
android/

# Build artifacts
dist/
web-build/
builds/
*.apk
*.ipa

# Signing / credentials
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig
credentials.json

# Backups
*.bak

# Editor
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Firebase
firebase-debug.log
firebase-debug.*.log
.firebase/
extensions/*.secret.local
extensions/*.env.local

# functions/ is npm-managed (see _packageManager note in functions/package.json).
# pnpm v11 doesn't honour the `overrides` block for rpc-websockets' uuid dep
# and bumps it to uuid@14 (ESM-only) → ERR_REQUIRE_ESM at deploy.
functions/pnpm-lock.yaml

# Claude Code worktrees
.claude/worktrees/
